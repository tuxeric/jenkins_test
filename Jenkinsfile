pipeline {
    agent any
    parameters {
	booleanParam(name: 'Produce_deb', defaultValue: false, description: '') 
    }
    stages {
	stage ('Build') {
	    }

	    environment {
		AWS_ACCESS_KEY_ID = 'dadada'
	    }
	    steps {
		sh 'pwd'
		sh 'id'
	    }
	}
    }
}
